name: build and run tests
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Install xmllint
      run: sudo apt-get update && sudo apt-get install -y llvm-15
    - uses: actions/checkout@v2
    - name: building 🏗️
      run: sh setup.sh
    - name: testing 🧪
      run: sh test.sh --skip-build
    - name: linting 💅
      run: sh lint.sh
